# Application
spring.application.name=inventory-management
server.port=8080
#server.address=0.0.0.0

# Local development tenant used only for requests sent directly to localhost.
# Override with DEFAULT_TENANT_SUBDOMAIN or an X-Tenant-* request header.
app.tenant.default-subdomain=${DEFAULT_TENANT_SUBDOMAIN:abc}

# CORS: allowed client origins (comma-separated).
# Reads from env ALLOWED_CLIENT_URLS, falls back to defaults.
client.allowed.urls=${ALLOWED_CLIENT_URLS:http://*.localhost:3000,http://43.204.141.135}

# MongoDB Configuration
# MongoDB connection (env override with defaults)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://mabinathperera_db_user:yWE159DFbPouJSqZ@labcluster.1wrbc22.mongodb.net/}
spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE:inventory_db}

# JWT Configuration
# WARNING: In production, externalize this secret using environment variables
# Example: jwt.secret=${JWT_SECRET:default-secret-key}
jwt.secret=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
jwt.expiration=86400000

# Logging
logging.level.com.inventory=DEBUG
logging.level.org.springframework.security=DEBUG

# OpenAI Configuration
spring.ai.openai.api-key=${OPENAI_API_KEY}
spring.ai.openai.chat.options.model=gpt-4o

app.query.url=${QUERY_URL:http://127.0.0.1:8000/query}